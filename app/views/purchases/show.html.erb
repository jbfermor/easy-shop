<div class="containr mt-2">
  <div class="row mt-2">
    <%= render @purchase %>
  </div>
  <div class="row mt-2 d-flex justify-content-center">
    <div class="row">
      <%= link_to "Editar", edit_purchase_path(@purchase), class: "btn btn-warning" %>
    </div>
    <div class="row">
      <%= button_to "Eliminar", @purchase, method: :delete, class: "btn btn-danger" %>
    </div>   
  </div>
    <div class="row mt-2">
      <%= link_to "Atrás", purchases_path, class: "btn btn-secondary" %>
    </div>
  </div>
</div>  