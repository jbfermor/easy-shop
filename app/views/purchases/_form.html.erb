<div class="container mt-2">
  <div class="row">
      <%= form_with(model: purchase) do |form| %>
        <% if purchase.errors.any? %>
          <div style="color: red">
            <h2><%= pluralize(purchase.errors.count, "error") %> prohibited this supplier from being saved:</h2>

            <ul>
              <% purchase.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="row mt-2">
          <%= form.label :purchase_date, class: "form-label" %>
          <%= form.date_field :purchase_date, class: "form-control" %>
        </div>

        <div class="row mt-2">
          <%= form.label "Proveedor", class: "form-label" %>
          <%= form.collection_select :supplier_id, @suppliers, :id, :name, {}, class: "form-select" %>
        </div>

        <div class="row mt-2"> 
            <%= form.submit "Guardar", class: "btn btn-success" %>
        </div>
      <% end %>
    </div>
